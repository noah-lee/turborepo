# PostgreSQL connection string. Required by the api and the migration runner.
DATABASE_URL=postgres://user:password@localhost:5432/turborepo

# API
NODE_ENV=development
HOST=0.0.0.0
PORT=3000
LOG_LEVEL=info
CORS_ORIGIN=*

# Auth — Google OAuth 2.0 (OIDC) client.
# Provision at: https://console.cloud.google.com/apis/credentials
#   → Create credentials → OAuth client ID → Application type: "Web application"
#   → Authorized redirect URI: must exactly match GOOGLE_REDIRECT_URI below.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:5173/api/auth/google/callback

# Auth — token signing secrets. Each must be at least 32 chars. Use independent values.
# Generate with: openssl rand -base64 48
ACCESS_TOKEN_SECRET=
REFRESH_TOKEN_SECRET=

# Auth — signs the short-lived oauth_state cookie. Generate with: openssl rand -base64 32
COOKIE_SECRET=

# Auth — where to send the browser after successful login (and after logout).
# Must be https in production.
WEB_ORIGIN=http://localhost:5173

# Stripe — billing. Create credentials at https://dashboard.stripe.com/apikeys
# In production, STRIPE_SECRET_KEY must start with sk_live_.
STRIPE_SECRET_KEY=
# Webhook signing secret for the endpoint configured at /api/billing/webhook.
# For local dev, run `stripe listen --forward-to localhost:3000/api/billing/webhook`
# and copy the printed `whsec_…` value here.
STRIPE_WEBHOOK_SECRET=
# Recurring price ID for the premium subscription tier (starts with price_).
STRIPE_PREMIUM_PRICE_ID=
